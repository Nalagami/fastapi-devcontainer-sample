repos:
  - repo: local
    hooks:
      - id: uv-task-check
        name: Run uv task check
        entry: uv run task check
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
